import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.suvila.post',
  appName: 'zip-zap-post',
  webDir: 'dist/post/browser'
};

export default config;
